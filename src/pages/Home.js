import React from "react";

const Home = () => {
  return (
    <div style={styles.container}>
      <h1>WELCOME</h1>
    </div>
  );
};

const styles = {
  container: {
    textAlign: "center",
    padding: "50px",
    fontSize: "2rem",
  },
};

export default Home;
